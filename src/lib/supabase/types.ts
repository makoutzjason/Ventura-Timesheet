// Placeholder until the schema exists in a real Supabase project. Once
// you've run the migration in supabase/migrations and created a project,
// regenerate this file with:
//
//   npx supabase gen types typescript --project-id <your-project-ref> > src/lib/supabase/types.ts
//
// Everything in src/lib/supabase/*.ts imports `Database` from here, so
// regenerating this one file is what turns on full autocomplete/type
// checking for every table.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
